# Ativa o modo de depuração (1 = True, 0 = False)
DEBUG=1

# Define a chave secreta usada pelo Django (substituir "foo" por uma chave mais segura em produção)
SECRET_KEY=foo

# Define os hosts permitidos para a aplicação Django (neste caso, localhost e IPv6 loopback)
DJANGO_ALLOWED_HOSTS=localhost 127.0.0.1 [::1]

# Configurações do banco de dados PostgreSQL
# Define o engine de banco de dados para PostgreSQL
SQL_ENGINE=django.db.backends.postgresql
# Define o nome do banco de dados
SQL_DATABASE=bookstore_dev_db
# Define o nome do usuário do banco de dados
SQL_USER=bookstore_dev
# Define a senha do banco de dados
SQL_PASSWORD=bookstore_dev
# Define o host do banco de dados (neste caso, o container 'db' em um ambiente Docker)
SQL_HOST=db
# Define a porta do banco de dados PostgreSQL (padrão: 5432)
SQL_PORT=5432
